# Changelog

## Unreleased

- **Changed:** Smaller `Powered by Qortal` logo assets are now used in the NuQloud website badge area and in the Nextcloud integration surfaces that previously referenced the larger full-size versions (migration/notes: website imports now use the smaller copied brand assets, and the integration login/Q-Apps header references now point to the 500px-wide variants).
- **Changed:** Terms of Service wording now also clarifies that standard NuQloud packages do not presently include direct hardware unless explicitly stated, explains nominative/comparative use of third-party service logos and marks, and states that `NuQloud for Nextcloud` is a Qortal-powered plugin extending the separate Nextcloud platform (migration/notes: `/terms` content expanded for clearer third-party, branding, and hardware-policy positioning).
- **Changed:** Terms of Service wording now more explicitly separates CHD-managed on-instance data from QDN-published data, clarifies that CHD cannot access, modify, or remove private data published to QDN, and explains that CHD redundancy guarantees apply only while the account remains active and in good standing (migration/notes: `/terms` content updated to reflect QDN control, privacy, and redundancy limits more precisely).
- **Changed:** Managed account plan cards now show the team-package option as a smaller outlined `team - up to 5 users` button instead of a plain text-style link, making the alternate multi-user package easier to spot (migration/notes: visual treatment and label updated for the team-version action in the plans section).
- **Changed:** Enlarged screenshot previews now include previous/next arrow controls so users can cycle through the gallery without closing the lightbox between images (migration/notes: lightbox navigation now reuses the active gallery state and wraps through the full screenshot set).
- **Changed:** The homepage screenshot gallery now reflows more cleanly on mobile by moving the screenshot navigation into a bottom tab strip beneath the active image instead of forcing users to scroll past the tab list first (migration/notes: mobile-only gallery navigation now renders inside the preview card area while desktop keeps the two-column selector layout).
- **Changed:** Internet-facing metadata now includes canonical/robots tags, richer Open Graph and Twitter card fields with absolute image URLs, and JSON-LD structured data so search engines and social/link previews can resolve NuQloud branding more reliably (migration/notes: `index.html` metadata was expanded for SEO and card-preview compatibility on `https://nuqloud.com/`).
- **Changed:** Homepage screenshot/gallery mappings were corrected again using the actual intended NuQloud UI captures, restoring the collaborative-doc/share slide, fixing the activity-history/Q-Mail/Q-Deck/conversation screenshots, and removing duplicate communication replacement logos by adding Telegram and WhatsApp to the communications comparison set (migration/notes: homepage gallery assets and replacement-icon lineup were remapped for the root landing route).
- **Changed:** The homepage screenshot gallery now uses the smaller curated image set the product requested, with the register-name and files/publishing screenshots remapped to the intended captions and extra one-off slides removed (migration/notes: gallery sequence/caption pairing corrected on the root landing route).
- **Changed:** The homepage plans area is now decluttered and reorganized into two matching lanes, with each `NuQloud Accounts` / `Dedicated Branded NuQloud Instances` summary card centered above its corresponding plan group, matching outline colors carried onto the plan cards, and `Buy Now` buttons themed to the same account-vs-dedicated accents (migration/notes: removed the redundant top `NuQloud Plans` heading/subtitle and the old model-summary CTA buttons from the plans section).
- **Changed:** The homepage `NuQloud Interface and Features` section now uses locally bundled real product screenshots with organized captions covering the dashboard, name registration, publishing flow, decentralized apps, conversations, and live collaboration (migration/notes: gallery placeholder cards were replaced with imported PNG assets so the section works in both internet-hosted and QDN-hosted builds).
- **Changed:** The `Choose Your NuQloud Model` comparison now sits inside the plans area above the plan groups, and the two model cards/buttons use distinct accent treatments so the hosted-account path and dedicated-branded path stand apart more clearly (migration/notes: homepage section order and visual emphasis refined on the root landing route).
- **Changed:** Homepage hero/about messaging now uses the new private-digital-world positioning, replacement-provider logos are larger bundled color icons without text labels, and the Qortal badge sits larger/further right on the decentralized publishing card (migration/notes: marketing-copy/layout polish on the root landing route; interface gallery content still awaiting real screenshots).
- **Changed:** Hosted-account plan naming is now uniform across the plans section, with `NuQloud Advanced` and `NuQloud Professional` replacing the shorter labels so the managed lineup reads consistently beside `NuQloud Starter` (migration/notes: copy-only naming cleanup in the homepage plans section).
- **Changed:** Terms of Service navigation now includes a clear `Home` link label back to the main NuQloud landing page instead of the older `NuQloud MSP` wording (migration/notes: navigation label cleanup on `/terms`).
- **Changed:** Plan cards now use a stronger `Buy Now` checkout CTA, a smaller `View Details` link to the non-checkout product page, and softer team-version product links; feature-story cards also now include direct `View Plans` jumps so users can move from capability sections into package selection more easily while scrolling (migration/notes: homepage action hierarchy and plan/product-link behavior refined).
- **Changed:** Homepage plan and contact actions now use the provided internet Paymenter checkout/ticket URLs directly instead of the previous placeholder plan-query flow; QDN-equivalent purchase links are still intentionally not wired because they are not ready yet (migration/notes: `App.tsx` now maps plan slugs to direct `payment.crowetic.com` checkout URLs and uses the ticket-creation URL for contact flow).
- **Changed:** Feature `Read More` pages were rebuilt into final-release detail views based on the preserved NuQloud archive content, with explicit product sections retained, placeholder/archive-explainer copy removed, and dead external/QDN-not-ready CTA links removed from the detail route (migration/notes: `FeatureDetailsPage` now uses the archived data as real page content rather than a temporary archive viewer).
- **Changed:** The homepage plans section now includes actual package pricing, storage allocations, decentralized publishing space, initial publishing-credit details, and branded-instance setup fees/capacity notes, with a shared explanation that core features remain the same across packages while storage and publishing allowances change by plan (migration/notes: `Plans` section data/layout expanded on the root landing route).
- **Fixed:** Shared NuQloud header/hero logos now use bundled module imports from `src/assets/brand` instead of root-relative `/public` paths, which keeps branding assets resolving correctly in QDN-hosted builds (migration/notes: `brandAssets.ts` switched to imported image modules).
- **Changed:** The ambiguous `Start Your Cloud` homepage CTA was removed from the hero and the top-bar action now scrolls directly to `Plans` instead of implying an immediate signup flow that does not exist yet (migration/notes: root landing-page CTA behavior simplified).
- **Changed:** Homepage plans now use `NuQloud Starter`, `Advanced`, and `Pro` for hosted accounts with `Team versions available` notices, while leftover site-planning copy was removed from the `Why`, `Model`, and `Foundation` sections and the feature-scene connector lines were dropped for a cleaner final presentation (migration/notes: marketing copy/layout cleanup on the root landing route).
- **Added:** Feature-story `Read More` actions now route to preserved in-depth detail pages at `/feature-details/:storyId`, built from the archived MSP landing-page content model instead of new throwaway copy (how to use: open any homepage feature story and select `Read More`).
- **Changed:** Homepage MSP copy now reflects the latest NuQloud wording updates across the hero quick-win panel, `What is NuQloud`, `What NuQloud Does`, plan labels, and final contact CTA (migration/notes: root landing route content updated; no API/runtime changes).
- **Changed:** Self-hosting/plugin route now explicitly notes that the plugin package is being finalized and will be released in the near future (migration/notes: copy-only update on `/self-hosting`).
- **Changed:** Feature-story `REPLACE` areas now keep all replaced providers visible together for readability, then apply one final cross-out state near the end of each story instead of rapidly cycling one provider at a time (migration/notes: homepage replacement-card interaction simplified).
- **Changed:** Feature-story progress now starts when each story stage first enters the viewport, so motion begins during visible entry instead of waiting for the card to settle deeper into the screen (migration/notes: homepage feature-story progress anchors now use viewport entry as the start point).
- **Changed:** Feature stories now begin moving sooner after the card settles, run over a broader pinned-window range, and complete before the full card handoff instead of finishing at the edge of visibility (migration/notes: homepage feature-story start/completion anchors retuned).
- **Changed:** Feature-story animation timing now begins earlier in the pinned scroll window and distributes bullet/replacement progression more evenly across each story instead of front-loading the action (migration/notes: homepage feature-story timing thresholds adjusted).
- **Changed:** Feature-story timing is now staged more explicitly: right-side workflow motion, left-side animated bullet list, and `REPLACE` provider takedowns no longer share the same narrow timing band, and the pinned cards/scene window are significantly larger (migration/notes: homepage feature-story sequencing and card sizing refined again).
- **Changed:** Feature-story cards now use animated check-mark bullet lists, a larger right-side workflow scene, and a faster sequential `REPLACE` feed so provider cards appear and get crossed out earlier during each story (migration/notes: homepage feature-story timing and layout refined).
- **Fixed:** Homepage feature-story cards now size and progress against the actual pinned-card travel, reducing dead space between stages and allowing the animations to complete within the visible viewport across more screen sizes (impact: smoother desktop/mobile storytelling in `What You Can Do With It`).
- **Changed:** Homepage `What You Can Do With It` is now a scroll-driven story section with larger sticky capability cards, animated workflow visuals, and big-tech replacement trays that open and cross out old-tool stacks as you progress (migration/notes: root landing route interaction and CSS updated; no route/API changes).
- **Changed:** MSP homepage now leads with a plain-language private cloud pitch, simplified section flow, and clearer separation between managed accounts and dedicated branded deployments (migration/notes: root landing route content and layout were restructured; self-hosting route unchanged).
- **Added:** Preserved the previous detailed MSP landing-page content model in `src/content/mspLegacyArchive.ts` for future reuse during later plan/detail page work (how to use: import archived content from that module instead of recovering old copy from git history).
- **Changed:** Public metadata/title for the NuQloud website now presents the service as a direct private-cloud offer rather than the previous abstract tagline (migration/notes: `index.html` social and browser metadata updated).
- **Changed:** Rebranded primary landing experience from Sovereign Cloud naming to NuQloud MSP and NuQloud for Nextcloud wording (migration/notes: metadata, top-bar labels, and route copy updated).
- **Changed:** MSP and plugin routes now use mobile-first tap-to-expand sections for dense content blocks to reduce excessive scrolling on phones (migration/notes: no data model changes; presentation-only behavior).
- **Changed:** Plugin route messaging was rewritten in more plain-language wording while preserving feature scope and reference links (migration/notes: no endpoint/runtime logic changes).
- **Changed:** Public metadata/title now presents NuQloud branding in social/link previews (migration/notes: `index.html` descriptions/titles updated).
- **Added:** Scroll-driven Sovereign Cloud landing page with hero, tiering, performance, architecture, add-ons, and roadmap sections (how to use: run `npm run dev` and open the root route).
- **Changed:** Theme system refined to branded dark/light blue palettes with runtime theme toggle and iframe compatibility (migration/notes: none).
- **Changed:** Runtime mode indicator for gateway-aware vs native context rendering (migration/notes: none).
- **Changed:** Public landing behavior now avoids forced auth on mount (migration/notes: `GlobalProvider` `authenticateOnMount` set to `false`).
- **Changed:** Added scroll-state choreography improvements (staggered reveals, parallax motion, active section tracking) for a smoother single-page flow (migration/notes: none).
- **Changed:** Deployment path section now uses conversion-focused CTA blocks for plugin-first and CHD managed-service audiences (migration/notes: none).
- **Changed:** Gateway/native runtime messaging now drives contextual primary CTA targeting and navigation behavior (migration/notes: none).
- **Changed:** Landing page width is no longer capped to a fixed max container, allowing wider viewport expansion while keeping responsive paddings (migration/notes: none).
- **Added:** Scroll-reactive geometric background objects on the right side to deliver a more dynamic visual identity without introducing heavy runtime dependencies (how to use: scroll through the page to see rotation/morph shifts).
- **Changed:** Access context detection now prioritizes trusted gateway host matching and falls back to `GET_USER_ACCOUNT` probing for native runtime detection (migration/notes: none).
- **Added:** Cross-functional link actions now open internet links in gateway context and copy those links in native runtime context (how to use: use the Access handling actions in the hero panel).
- **Changed:** Context notice is now visually separated as an informational access banner so it does not read as a service-tier feature (migration/notes: none).
- **Changed:** Background grid was reduced in visual intensity with a darker base, wider spacing, center emphasis, and edge fade to reduce distraction (migration/notes: none).
- **Changed:** Experience split into two explicit scroller routes: MSP-primary (`/`) and self-hosting plugin (`/self-hosting`) with separate messaging and navigation (migration/notes: route added).
- **Added:** Self-hosting route visual identity uses left-side animation and circular-grid black-background style while MSP route keeps right-side animation and square-grid atmosphere (how to use: navigate between routes using top nav links).
- **Changed:** Gateway detection is now strict: authenticated `GET_USER_ACCOUNT` always means QDN mode; trusted gateway mode requires `*.crowetic.com`; all other access is internet fallback context (migration/notes: external-link behavior now follows context mode).
- **Fixed:** QDN detection now retries for delayed bridge initialization in Hub/dev mode, so authenticated `GET_USER_ACCOUNT` can correctly override initial internet/gateway assumptions (impact: prevents false “internet” context on startup).
- **Changed:** External links now open directly only in gateway/internet contexts and copy to clipboard in authenticated QDN context (migration/notes: applies to payment/docs buttons).
- **Changed:** Main content rail is intentionally narrower by percentage to expose animated side objects while remaining responsive (migration/notes: none).
- **Changed:** MSP page content flow is now marketing-oriented: `Replaces the Following` → `Unify Your Cloud Life` → full replacement detail cards with stronger narrative progression (migration/notes: none).
- **Changed:** Removed MSP `Managed Operations Model` section and replaced it with customer-outcome focused replacement detail content (migration/notes: none).
- **Changed:** `Two Paths` became `Two Starting Points` to clearly position per-user existing-cloud onboarding vs fully private branded cloud onboarding (migration/notes: none).
- **Added:** Side-slide reveal choreography for detailed replacement cards to create stronger scroll storytelling without overwhelming density (how to use: scroll through the MSP details section).
- **Added:** Replacement cards now include provider icon references and staged panel reveal timing in the MSP `Replaces` section (how to use: scroll through the replacements section and observe timed card entry).
- **Fixed:** Replacement icons now load from bundled local SVG assets instead of remote CDN URLs (impact: resolves icon loading failures in QDN/runtime environments).
- **Changed:** Terminology shifted away from “Q-Apps” toward “distributed off-network applications” with expanded messaging on decentralized app access, encrypted chunked off-network backups, redundancy guarantees, private communications options, and distributed publishing status (migration/notes: content copy update).
- **Added:** Deep-dive interactions now expand contextual panels directly below the originating section instead of always linking out (how to use: click any `Deep Dive` button on MSP page).
- **Changed:** `Two Starting Points` now renders as vertical stacked offers, each with 3 package cards and dual payment actions (USD + QORT) (migration/notes: QORT path opens/copies QDN-oriented checkout links).
- **Fixed:** Replacement-detail deep dives now open inline under the clicked card (instead of detached bottom panel layout) with corrected spacing and fuller explanatory content (impact: resolves stretched panel/misaligned spacing issues).
- **Changed:** MSP feature messaging now reflects that core capabilities are included across every package path; in-page package cards no longer show fixed price copy and now emphasize deployment/capacity differences (migration/notes: pricing moved out of this marketing view).
- **Added:** Full included-feature matrix content plus expansion-module and roadmap sections for broader replacement coverage (how to use: open `Two Starting Points` and click `Full Details`).
- **Changed:** Replacements section now includes a wider big-tech mapping set (workspace, comms, storage, workflow, docs/knowledge, groupware, identity, operations, and lifecycle funnel equivalents) (migration/notes: content model expanded).
- **Changed:** `Unify Your Cloud Life` now renders as a titled staggered sequence with stronger scroll-stage readability (migration/notes: section presentation updated).
- **Changed:** Included/Expansion/Roadmap content moved under `Two Starting Points` and is now toggled via `Full Details` to reduce top-level section density (migration/notes: moved from matrix block to starting-points block).
- **Changed:** Expansion modules narrowed to `Branded client apps`, `Enterprise federation`, `SaaS onboarding and migration`, and `Custom distributed applications`; roadmap now tracks `CommOps`, `Workspace CRM`, and `Expanded sovereign communications options` (migration/notes: module taxonomy revised).
- **Changed:** Checkout buttons now render by access context: QDN shows QORT-only actions, gateway/internet shows USD-only actions (migration/notes: prevents non-working checkout options from being shown in the current mode).
- **Changed:** Hero CTAs renamed to `See Packages` and `Details` with direct section navigation targets (`starting-points` and `capabilities`) (migration/notes: replaces checkout-based hero CTA behavior).
- **Changed:** Active section tracking for side progress dots now uses visible section-center proximity for tighter alignment with on-screen content (migration/notes: scroll-state calculation updated).
- **Changed:** Internet-hosted metadata/title/fallback branding no longer uses default Q-App identity; site metadata now presents Sovereign Cloud / CHD branding (migration/notes: `index.html` meta/title updated).
- **Added:** New `/terms` route with CHD Terms of Service content adapted for the sovereign provider model (how to use: open `/terms` from footer link).
- **Added:** Global footer with `© 2026 CHD` and `Terms of Service` link across site routes (how to use: available at the bottom of every page).
